import { AddedpartsFilterPipe } from './addedparts-filter.pipe';

describe('AddedpartsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AddedpartsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
